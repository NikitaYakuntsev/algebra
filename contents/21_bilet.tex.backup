\subsection{Билет 21}

\textit{Норма. Линейное нормированное пространство. Метрика. Метрическое пространство.}

Пусть $E$ - линейное пространство над полем $R$ (или $C$). 

Функция $f:E\to R: x \in E \to \| x \| \in R$ называется нормой, если:
\begin{enumerate}
 \item \begin{enumerate}
        \item $\|x\| \ge 0$
        \item $\|x\| = 0 \Leftrightarrow x = 0$
       \end{enumerate}
 \item $\forall x \in E ~ \forall \alpha \in R ~ \|\alpha x \| = \left| \alpha \right| \|x\|$
 \item Неравенство треугольника: $\|x+y\| \le \|x\| + \|y\|$
\end{enumerate}

При этом, $E$ - линейное нормированное пространство.

Примеры:

\begin{enumerate}
 \item $E = R: \forall x \in R \|x\| = \left| x \right|$
 \item $E = V^3: \|x\| = \sqrt{(x,x)}$
 \item $E = R^n: x = \left(\begin{matrix}
                            \mbox{$\xi_1$}\\
                            \mbox{$\ldots$}\\
                            \mbox{$\xi_n$}
                           \end{matrix}\right) \|x\| = \max \left|\xi_i\right|$
       Проверим:
       \begin{enumerate}
        \item \begin {enumerate}
		\item $\|x\| = \max \left|\xi_i\right| \ge 0$ 
		\item $\|x\| = \max \left|\xi_i\right| = 0 \Rightarrow \forall i = 1, \ldots, n ~ \left|\xi_i\right| = 0 \Rightarrow x = 0$
	      \end{enumerate}
	\item $\|\alpha x \| = \max \left|\alpha \xi_i\right| = \max \left|\alpha\right| \left|\xi_i\right| = \left|\alpha\right| \max  \left|\xi_i\right| = \left|\alpha\right| \|x\|$
        \item $ y = \left(\begin{matrix}
                            \mbox{$\chi_1$}\\
                            \mbox{$\ldots$}\\
                            \mbox{$\chi_n$}
                           \end{matrix}\right)$
              $\|x+y\| = \max \left|\xi_i + \chi_i\right| \le \max \left|\xi_i\right| + \left|\chi_i\right| = \max \left|\xi_i\right| + \max \left|\chi_i\right| = \|x\|+\|y\|$
       \end{enumerate}
 \item $\|x\| = \sum_{i=1}^n \left| \xi_i \right|$
       \begin{enumerate}
        \item \begin {enumerate} 
		\item $\|x\| = \sum_{i=1}^n \left| \xi_i \right| \ge 0$
		\item $\|x\| = \sum_{i=1}^n \left| \xi_i \right| = 0 \Rightarrow \forall i = 1, \ldots, n ~ \left| \xi_i \right| = 0 \Rightarrow x = 0$
	      \end{enumerate}
        \item $\|\alpha x \| = \sum_{i=1}^n \left|\alpha \xi_i \right| = \left|\alpha\right| \sum_{i=1}^n \left| \xi_i \right| = \left|\alpha\right| \|x\|$
        \item $\|x+y\| = \sum_{i=1}^n \left|\xi_i + \chi_i \right| \le \sum_{i=1}^n \left|\xi_i\right| + \sum_{i=1}^n \left|\chi_i \right| = \|x\|+\|y\|$
       \end{enumerate}
       
\end{enumerate}

\textbf{Теорема 1} Любое евклидово пространство является нормарованным с нормой $\|x\| = \sqrt{(x,x)}$

\textbf{Доказательство:} 

\begin{enumerate}
 \item \begin{enumerate}
        \item $(x,x) \ge 0 \Rightarrow \|x\| = \sqrt{(x,x)} \ge 0$
        \item $x = 0 \Rightarrow (x,x) = 0 \Rightarrow \|x\| = 0$
        
        $\|x\| = 0 \Rightarrow \sqrt{(x,x)} = 0 \Rightarrow (x,x) = 0 \Rightarrow x = 0$
       \end{enumerate}
 \item $\|\alpha x \| = \sqrt{(\alpha x, \alpha x)} = \sqrt{\alpha^2 (x,x)} = \left| \alpha \right| \sqrt{(x,x)} = \left| \alpha \right|\|x\|$
 \item $\|x+y\|^2 = (x+y,x+y) = (x,x) + 2(x,y) + (y,y) \le (x,x) + 2\sqrt{(x,x)(y,y)} + (y,y) = \|x\|^2 + 2\|x\|*\|y\| +\|y\|^2 = (\|x\| + \|y\|)^2$
 
 $\|x+y\|^2 \le (\|x\| + \|y\|)^2$ - извлечем корень
 
 $\|x+y\|\le \|x\| + \|y\|$
\end{enumerate}

$\rho(x,y) = \|x-y\|$ - расстояние от $x$ до $y$

Линейное пространство $E$ называют метрическим, если на нем задана функция $\rho:E\times E \to R; x,y \in E \to \rho(x,y) \in R$, удовлетворяющая свойствам:

\begin{enumerate}
 \item \begin{enumerate}
	\item $\forall x,y \in E ~ \rho(x,y) \ge 0$
	\item $\rho(x,y) = 0 \Leftrightarrow x = y$ 
       \end{enumerate}
 \item $\forall x,y \in E ~ \rho(x,y) = \rho(y,x)$
 \item $\forall x,y,z \in E ~ \rho (x,z) \le \rho (x,y) + \rho (y,z)$
\end{enumerate}


